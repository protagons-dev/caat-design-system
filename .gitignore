# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/

# Environment files
.env
.env.local
.env.*.local

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker volumes / temp
docker/data/

# Local archives
design-sys.zip

# Prototype build artifacts — regenerated locally; keep binaries out of git.
# (Served locally via the ./public volume mount; not committed to avoid bloat.)
prototype/
CAAT-navigation-prototype.zip
